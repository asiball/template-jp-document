---
title: "仕様書の書き方クイックスタート"
---

<!--
  非技術者(Markdown 初心者の PM・品証など)向けの最短手順です。
  Typst のデバッグ方法やフォント解決など開発者向けの話題はここには書きません。
  詳しい執筆ルール・ビルドの仕組みは README.md を参照してください。
-->

# はじめに

本書は、`template-jp-document` を使って Markdown から仕様書 PDF を作るための、非技術者向けの最短手順です。Markdown に不慣れな PM・品証の方を対象としています。執筆ルールやビルドの仕組みの詳細は `README.md` を参照してください。

## sample-spec.md をコピーして書き始める

一から書き始めるのではなく、見本の `docs/sample-spec.md` をコピーして、中身を自分の仕様書の内容に置き換えるのが最も早い方法です。

```sh
cp docs/sample-spec.md docs/自分の仕様書.md
```

見出し・表・コードブロックといった「型」はすでに見本の中にあるので、内容だけを書き換えれば体裁は自動的に整います。

## 冒頭の YAML メタデータを書く

ファイル先頭の `---` で囲まれた部分に、表紙・ヘッダに表示する情報を書きます。

| 変数 | 必須 | 内容 |
|---|---|---|
| `title` | 必須 | 表紙に大きく表示するタイトル |
| `subtitle` | 任意 | サブタイトル |
| `docnumber` | 任意 | 文書番号 |
| `version` | 任意 | 版数 |
| `date` | 任意 | 発行日 |
| `author` | 任意 | 作成者 |
| `organization` | 任意 | 発行組織名 |
| `revisions` | 任意 | 改訂履歴(バージョン・日付・作成者・変更内容の一覧) |

各変数の詳しい説明は `README.md` の「メタデータ(YAML フロントマター)一覧」を参照してください。

**補足**: `revisions`(改訂履歴)が版を重ねて長くなってきたら、`docs/自分の仕様書.revisions.md` という別ファイルに切り出せます。中身は普段の Markdown の表と同じ書き方で、改訂を重ねるたびに末尾へ 1 行足していくだけです(ファイルを置けば自動的に読み込まれます)。

```markdown
| 版数 | 日付 | 作成者 | 改訂内容 |
|---|---|---|---|
| 1.0 | 2026-05-01 | 山田太郎 | 初版作成 |
| 1.1 | 2026-06-10 | 鈴木花子 | 在庫一覧の章を追加 |
```

このとき、仕様書本体のフロントマターからは `revisions:` の項目を削除してください(両方に書くと別ファイル側が無視されます)。詳しくは `README.md` の「改訂履歴の別ファイル化」を参照してください。

## 見出しを書く(番号は自動)

`#` の数がそのまま見出しの階層になります。

```markdown
# 章のタイトル
## 節のタイトル
### 項のタイトル
```

**番号は自分で書かないでください**。`# 1. はじめに` のように自分で番号を振ると、PDF 化したときに「1 1. はじめに」のように番号が二重になってしまいます。番号は PDF 化の際に自動で付きます。

## 表・コード・図・脚注の最小例

表は Markdown 標準の書き方でそのまま書けます。

```markdown
| 項目 | 内容 |
|---|---|
| 対象読者 | 非技術者の PM・品証 |
```

コードブロックは、開始と終了をバッククォート3つで囲み、直後に言語名を書きます。

```json
{ "example": true }
```

図は `/assets/images/` に PNG を置き、リポジトリルートからの絶対パスで参照します。

```markdown
![構成図の説明文](/assets/images/example.png)
```

脚注は本文中に番号を置き、同じ番号の内容を文末にまとめて書きます。

```markdown
本文中の注記です[^1]。

[^1]: 脚注の内容。
```

## ビルドして PDF を確認する

自分のパソコンに pandoc / typst が入っていれば、次のコマンドで PDF を作れます。

```sh
make pdf SRC=docs/自分の仕様書.md
```

環境構築が難しい場合は、開発メンバーやビルド担当者に「`docs/自分の仕様書.md` をビルドしてほしい」と依頼してください。

## やってはいけないこと

- **フォント・色・余白などのスタイル指定を書かない**。太字・表・コードブロックといった「構造」だけを書けば、見た目は自動的に整います。見た目そのものを変えたい場合は開発メンバーに相談してください。
- **見出しに自分で番号を振らない**(前述のとおり、自動採番と二重になります)。

## もっと詳しく知りたいときは

執筆ルールの詳細、メタデータの全項目、エスケープハッチ(表のセル結合など特殊な場合の書き方)は `README.md` を参照してください。
